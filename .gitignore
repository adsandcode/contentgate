__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.venv/
venv/

node_modules/
mcp/dist/
*.tsbuildinfo

# Generated by mcp/scripts/sync-fixtures.mjs at build time so that npm pack
# includes it. Keeping a second copy under version control invites the two
# from drifting apart, which is the exact failure the shared file prevents.
mcp/fixtures/

.contentgate-lastmod.json
.DS_Store

# Real configuration and the leak scanner. Never published.
.private/
